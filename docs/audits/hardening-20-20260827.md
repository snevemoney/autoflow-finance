# Hardening audit (dry-run) — 20-point checklist

- **Date:** 2026-08-27
- **Scope:** `main` at `a79a5d4` (report-only; no application behavior change)
- **Excluded:** `node_modules`, lockfiles (`package-lock.json`, `bun.lockb`), build output (`dist`)
- **Stack:** Vite + React SPA, Supabase (PostgREST + 2 Edge Functions), no Next.js/Express route handlers

Status values: **proposed** = finding(s) to address later; **none found** = check clean.

---

## 1. Duplicate utilities

**Status:** proposed · **Count:** 12

1. Dual toast APIs. Most UI uses `toast({ title, description })` from `@/hooks/use-toast`; one call site uses `sonner`.
   - `src/hooks/use-toast.ts:137` (`toast` function)
   - `src/components/deals/UnmatchedExtractionRow.tsx:13` (`import { toast } from 'sonner'`)
   - `src/components/deals/UnmatchedExtractionRow.tsx:94` (`toast.success`)
   - `src/components/deals/UnmatchedExtractionRow.tsx:98` (`toast.error`)
2. Two toaster roots mounted together.
   - `src/App.tsx:28` (`<Toaster />` from shadcn)
   - `src/App.tsx:29` (`<Sonner />`)
3. Re-export shim of the same toast module.
   - `src/components/ui/use-toast.ts:1-3`
   - `src/hooks/use-toast.ts:186`
4. `calcMonthlyFromExtraction` copied verbatim.
   - `src/components/deals/IncomeVerificationCard.tsx:35-43`
   - `src/components/deals/UnmatchedExtractionRow.tsx:36-44`
5. Same frequency→monthly math as a multiplier table.
   - `src/components/deals/IncomeCalculator.tsx:18-23` (`FREQUENCY_MULTIPLIERS`)
6. `IncomeSource` / `IncomeSourceType` / `IncomeVerificationStatus` / `CalcMethod` defined twice.
   - `src/types/deal.ts:225-259`
   - `src/components/deals/IncomeSourceCard.tsx:10-42`
   - `src/components/deals/IncomeCalculator.tsx:14` (`export type CalcMethod`)
7. Extracted-income row shapes duplicated (not a shared type).
   - `src/components/deals/ExtractedDataBadge.tsx:10-21`
   - `src/components/deals/IncomeVerificationCard.tsx:21-33`
   - `src/components/deals/UnmatchedExtractionRow.tsx:16-28`
   - `src/components/deals/IncomeDocPreview.tsx:16-25`
   - `src/components/deals/IncomeSourceCard.tsx:44-52` (`LinkedExtraction`)
8. Edge Function CORS + 429/402 error handling copied.
   - `supabase/functions/extract-income-data/index.ts:3-7,120-132`
   - `supabase/functions/verify-employer/index.ts:3-7,90-102`
9. `INCOME_DOC_TYPES` duplicated.
   - `src/components/deals/DocumentUpload.tsx:29`
   - `src/pages/DealDetail.tsx:103`
10. Dual deal lists: live `useDeals` vs mock `getDealsByDepartment`.
    - `src/hooks/use-deals.ts:221-226`
    - `src/data/mockData.ts:412-416`
    - Live: `src/pages/DealsList.tsx:21`, `src/pages/Pipeline.tsx:59`
    - Mock: `src/pages/CreditQueue.tsx:18`, `src/pages/IncomeQueue.tsx:16`, `src/pages/FundingQueue.tsx:16`
11. Frequency labels duplicated.
    - `src/components/deals/IncomeCalculator.tsx:25-30` (`FREQUENCY_LABELS`)
    - `src/components/deals/IncomeDocPreview.tsx:68-73` (`FREQ_LABELS`)
12. PTI/DTI math copied across cards/queues.
    - `src/components/deals/DealSummaryCard.tsx:37-42,66-69`
    - `src/components/deals/DealCard.tsx:120`
    - `src/components/deals/IncomeVerificationCard.tsx:265-300,534-542`
    - `src/pages/CreditQueue.tsx:54-56`

---

## 2. Secrets in config

**Status:** proposed · **Count:** 1

File/line/kind only (no secret values):

| File | Line | Kind |
|---|---|---|
| `.env` | 2 | Supabase anon/publishable JWT (`VITE_SUPABASE_PUBLISHABLE_KEY`). `.env` is git-tracked. |

Notes (not counted as extra secrets): `.env:1` project id and `.env:3` project URL are identifiers, not credentials. Edge Functions read `LOVABLE_API_KEY` from `Deno.env` (`supabase/functions/extract-income-data/index.ts:24`, `supabase/functions/verify-employer/index.ts:24`) and do not hardcode it. The anon key is a publishable key by design; the finding is that the env file containing it is committed.

---

## 3. Functions over 400 lines

**Status:** proposed · **Count:** 3

Brace-depth measure of the exported component function body:

| Lines | Function | Location |
|---|---|---|
| 997 | `IncomeCalculator` | `src/components/deals/IncomeCalculator.tsx:65-1061` |
| 582 | `DealDetail` | `src/pages/DealDetail.tsx:49-630` |
| 506 | `IncomeVerificationCard` | `src/components/deals/IncomeVerificationCard.tsx:59-564` |

Next largest under the limit: `Settings` 274 (`src/pages/Settings.tsx:25-298`), `DocumentUpload` 269 (`src/components/deals/DocumentUpload.tsx:44-312`), `handleApply` 226 (`src/components/deals/IncomeCalculator.tsx:229-454`).

---

## 4. Components over 200 lines

**Status:** proposed · **Count:** 15

Application components (file line count; component starts at listed line):

| Lines | Component | Location |
|---|---|---|
| 1061 | `IncomeCalculator` | `src/components/deals/IncomeCalculator.tsx:65` |
| 630 | `DealDetail` | `src/pages/DealDetail.tsx:49` |
| 564 | `IncomeVerificationCard` | `src/components/deals/IncomeVerificationCard.tsx:59` |
| 312 | `DocumentUpload` | `src/components/deals/DocumentUpload.tsx:44` |
| 307 | `IncomeDocPreview` | `src/components/deals/IncomeDocPreview.tsx:75` |
| 298 | `Settings` | `src/pages/Settings.tsx:25` |
| 258 | `IncomeSourceCard` | `src/components/deals/IncomeSourceCard.tsx:89` |
| 257 | `ApplicantDebtsCard` | `src/components/deals/ApplicantDebtsCard.tsx:62` |
| 237 | `AddIncomeSourceDialog` | `src/components/deals/AddIncomeSourceDialog.tsx:38` |
| 219 | `Reports` | `src/pages/Reports.tsx:33` |
| 215 | `DealCard` | `src/components/deals/DealCard.tsx:23` |

shadcn/ui generated files over 200:

| Lines | File |
|---|---|
| 637 | `src/components/ui/sidebar.tsx:1` |
| 303 | `src/components/ui/chart.tsx:1` |
| 224 | `src/components/ui/carousel.tsx:1` |
| 207 | `src/components/ui/menubar.tsx:1` |

---

## 5. Dead code

**Status:** proposed · **Count:** 42

**Unused application modules / exports**

- `src/pages/Index.tsx:3-7` — not registered in `src/App.tsx` routes
- `src/components/NavLink.tsx:11-28` — no importers
- `src/components/ui/use-toast.ts:1-3` — unused re-export (`toaster.tsx` imports `@/hooks/use-toast`)
- `src/hooks/use-deals.ts:236` `useDealsByStatus` — no importers
- `src/hooks/use-deals.ts:244` `useDealsByDepartment` — no importers
- `src/hooks/use-deals.ts:253` `useDealers` — no importers
- `src/data/mockData.ts:404` `getDealsByStatus` — no importers (Pipeline defines a local function of the same name at `src/pages/Pipeline.tsx:71`)
- `src/data/mockData.ts:408` `getDealById` — no importers
- `src/types/deal.ts:225-259` `IncomeSourceType`, `IncomeVerificationStatus`, `CalcMethod`, `IncomeSource` — never imported from `@/types/deal` (live copies live on `IncomeSourceCard`)
- `src/types/deal.ts:261` `ResidencyStatus` — no importers
- `package.json:62` `zod` and `package.json:14` `@hookform/resolvers` — no `zod` / `safeParse` usage in app source

**Unused shadcn/ui (no import from application code; several only referenced by other unused UI)**

- `src/components/ui/accordion.tsx:1`
- `src/components/ui/alert-dialog.tsx:1`
- `src/components/ui/alert.tsx:1`
- `src/components/ui/aspect-ratio.tsx:1`
- `src/components/ui/avatar.tsx:1`
- `src/components/ui/breadcrumb.tsx:1`
- `src/components/ui/calendar.tsx:1`
- `src/components/ui/carousel.tsx:1`
- `src/components/ui/chart.tsx:1`
- `src/components/ui/collapsible.tsx:1`
- `src/components/ui/command.tsx:1`
- `src/components/ui/context-menu.tsx:1`
- `src/components/ui/drawer.tsx:1`
- `src/components/ui/form.tsx:1`
- `src/components/ui/hover-card.tsx:1`
- `src/components/ui/input-otp.tsx:1`
- `src/components/ui/menubar.tsx:1`
- `src/components/ui/navigation-menu.tsx:1`
- `src/components/ui/pagination.tsx:1`
- `src/components/ui/progress.tsx:1`
- `src/components/ui/radio-group.tsx:1`
- `src/components/ui/resizable.tsx:1`
- `src/components/ui/scroll-area.tsx:1`
- `src/components/ui/sidebar.tsx:1` (only consumer of `useIsMobile`)
- `src/hooks/use-mobile.tsx:5`
- `src/components/ui/sheet.tsx:1` (only imported by unused `sidebar.tsx`)
- `src/components/ui/skeleton.tsx:1` (only imported by unused `sidebar.tsx`)
- `src/components/ui/separator.tsx:1` (only imported by unused `sidebar.tsx`)
- `src/components/ui/slider.tsx:1`
- `src/components/ui/toggle.tsx:1`
- `src/components/ui/toggle-group.tsx:1`

---

## 6. Silent / empty catch

**Status:** proposed · **Count:** 10

Empty / swallow-without-user-feedback:

1. `src/components/deals/DroppableInput.tsx:50-52` — `catch { // ignore invalid drops }`
2. `src/pages/DealDetail.tsx:113-129` — `autoDecline` only toasts `if (!error)`; failures are silent
3. `src/components/deals/IncomeVerificationCard.tsx:128-130` — insert `.then(({ error }) => { if (!error) refetchSources(); })`
4. `src/components/deals/IncomeVerificationCard.tsx:149-152` — update `.then(() => {})` discards error
5. `src/components/deals/IncomeVerificationCard.tsx:176-182` — update `.then(() => refetchSources())` ignores error
6. `src/pages/CreditQueue.tsx:28-32` — `.then(({ data }) => { if (!data) return; })` no `error` branch
7. `src/components/deals/IncomeDocPreview.tsx:166-167` — signed URL `if (!error && data?.signedUrl)`; failure leaves empty preview
8. `src/components/layout/AppHeader.tsx:36-43` — profile fetch `.then(({ data }) => …)` ignores `error`
9. `src/components/deals/ApplicantDebtsCard.tsx:117-126` — `deleteMutation` has `onSuccess` toast, no `onError`
10. `src/pages/NotFound.tsx:7-8` — 404 only `console.error`, no in-app error surface (minor)

Not counted: catch blocks that `console.error` **and** toast (`EmployerVerificationCard.tsx:69-75`, `DocumentUpload.tsx:112-121`, `AddIncomeSourceDialog.tsx:111-112`, `IncomeSourceActions.tsx:81-82`, `IncomeCalculator.tsx:449-450,473-474,883-884`, `UnmatchedExtractionRow.tsx:96-98`, both Edge Functions).

---

## 7. UI API calls missing loading / error

**Status:** proposed · **Count:** 18

(Sites that already pair loading + error UI are omitted: Auth, EmployerVerificationCard, DocumentUpload extraction, AddIncomeSourceDialog, IncomeSourceActions, UnmatchedExtractionRow, IncomeCalculator apply/docs, ApplicantDebtsCard add mutation.)

| Location | Loading | Error UI |
|---|---|---|
| `src/pages/DealsList.tsx:21` `useDeals()` | yes (`isLoading` :99) | no `isError` |
| `src/pages/Pipeline.tsx:59` `useDeals()` | yes (`:89`) | no `isError` |
| `src/pages/DealDetail.tsx:52` `useDeal(id)` | yes (`:135`) | no `isError` (only empty `!deal` :144) |
| `src/pages/DealDetail.tsx:57-71` extracted-income query | no | no |
| `src/pages/DealDetail.tsx:74-86` income-sources query | no | no |
| `src/pages/DealDetail.tsx:89-101` applicant-debts query | no | no |
| `src/components/deals/IncomeVerificationCard.tsx:61-71` debts | no | no |
| `src/components/deals/IncomeVerificationCard.tsx:81-92` sources | no | no |
| `src/components/deals/IncomeVerificationCard.tsx:95-105` extractions | no | no |
| `src/components/deals/IncomeDocPreview.tsx:81-91` | no | no |
| `src/components/deals/IncomeDocPreview.tsx:93-104` | no | no |
| `src/components/deals/IncomeDocPreview.tsx:107-117` | no | no |
| `src/components/deals/IncomeDocPreview.tsx:120-131` | no | no |
| `src/components/deals/IncomeDocPreview.tsx:151-171` signed URL | spinner only (`loadingPreview`) | silent fail |
| `src/pages/CreditQueue.tsx:24-67` debts fetch | no | no |
| `src/components/layout/AppHeader.tsx:34-43` profile | no | no |
| `src/components/deals/ApplicantDebtsCard.tsx:75-86` list query | empty-state only (`:173`) | no `isError` |
| `src/components/deals/ApplicantDebtsCard.tsx:117-126` delete | no pending/error UI | no `onError` |

`useQuery` does throw on PostgREST error, but no page reads `isError` / `error` to render a message.

---

## 8. DB queries in route handlers

**Status:** proposed · **Count:** 2

No Next.js `app/api`, Express, or other server route handlers exist. The SPA route pages below issue PostgREST queries inline (data-access not isolated to `use-deals.ts`):

1. `src/pages/DealDetail.tsx:61-64` `.from('extracted_income_data').select('*')`
2. `src/pages/DealDetail.tsx:78-81` `.from('income_sources').select('*')`
3. `src/pages/DealDetail.tsx:93-96` `.from('applicant_debts').select('*')`
4. `src/pages/DealDetail.tsx:114-121` `.from('deals').update(...)` (side effect in the page)
5. `src/pages/CreditQueue.tsx:28-31` `.from('applicant_debts').select(...)`

Counted as **2** route pages. Additional DB access lives in UI components (not route handlers): `ApplicantDebtsCard`, `IncomeVerificationCard`, `IncomeCalculator`, `IncomeDocPreview`, `AddIncomeSourceDialog`, `IncomeSourceActions`, `UnmatchedExtractionRow`, `EmployerVerificationCard`, `AppHeader`. Edge Functions do **not** query the database.

---

## 9. Sync I/O in request handlers

**Status:** none found · **Count:** 0

No `readFileSync` / `writeFileSync` / `existsSync` / other `*Sync` fs calls. `FileReader.readAsDataURL` in `src/components/deals/DocumentUpload.tsx:32-41` is async. Edge Functions use `await req.json()` / `await fetch`.

---

## 10. List endpoints without pagination

**Status:** proposed · **Count:** 12

No `.range()`, `.limit()`, or page tokens on list reads. `src/components/ui/pagination.tsx` is unused.

1. `src/hooks/use-deals.ts:161-165` `from('deals').select(DEAL_SELECT)` all rows
2. `src/hooks/use-deals.ts:212-216` `from('dealers').select('*')` all rows
3. `src/pages/DealDetail.tsx:61-64` `extracted_income_data` by deal, unbounded
4. `src/pages/DealDetail.tsx:78-81` `income_sources` by deal, unbounded
5. `src/pages/DealDetail.tsx:93-96` `applicant_debts` by deal, unbounded
6. `src/pages/CreditQueue.tsx:28-31` `applicant_debts` `.in('deal_id', dealIds)`, unbounded
7. `src/components/deals/ApplicantDebtsCard.tsx:78-82` `applicant_debts` select `*`
8. `src/components/deals/IncomeVerificationCard.tsx:64-67` `applicant_debts` select `*`
9. `src/components/deals/IncomeVerificationCard.tsx:84-88` `income_sources` select `*`
10. `src/components/deals/IncomeVerificationCard.tsx:98-101` `extracted_income_data` select `*`
11. `src/components/deals/IncomeDocPreview.tsx:96-100` `documents` select `*` for deal
12. `src/components/deals/IncomeDocPreview.tsx:123-127` unlinked extractions for deal

Single-row reads (`useDeal`, `maybeSingle` profile) are not list endpoints and are not counted.

---

## 11. Inconsistent API shapes

**Status:** proposed · **Count:** 8

1. Toast payload: object `{ title, description, variant }` (`src/hooks/use-toast.ts:137`) vs `toast.success(string)` (`src/components/deals/UnmatchedExtractionRow.tsx:94`).
2. Deal list source: camelCase `Deal` from `transformDeal` (`src/hooks/use-deals.ts:78-152`) vs snake_case `IncomeSource` / `ApplicantDebt` passed straight from PostgREST (`src/components/deals/IncomeSourceCard.tsx:13`, `src/components/deals/ApplicantDebtsCard.tsx:18`).
3. Live deals (`useDeals`) on `/deals` and `/pipeline` vs mock arrays on `/`, `/credit`, `/income`, `/funding`, `/dealers`, `/users`, `/reports` (`src/pages/Dashboard.tsx:5`, `src/pages/CreditQueue.tsx:3`, etc.).
4. Edge success body is a raw object (`extract-income-data/index.ts:147`, `verify-employer/index.ts:117`); errors are `{ error: string }` (`:19`, `:153`). Client `functions.invoke` wraps as `{ data, error }`.
5. Query-key split for the same table: `['applicant-debts', deal?.id]` (`DealDetail.tsx:90`) vs `['applicant-debts', dealId]` (`ApplicantDebtsCard.tsx:76`) happen to match; income sources do **not**: `['income-sources-detail', deal?.id]` (`DealDetail.tsx:75`) vs `['income-sources', deal.id]` (`IncomeVerificationCard.tsx:82`).
6. `IncomeSource` on the card omits `benefit_cap_applied` (`IncomeSourceCard.tsx:13-42`) while `src/types/deal.ts:258` includes it.
7. RLS: some policies use `TO authenticated` (`income_sources` in `supabase/migrations/20260214005858_080194cb-173a-4fdc-a37d-4a8897292565.sql:35-38`); `extracted_income_data` and `applicant_debts` policies omit the role (`20260213235900_66b162ae-b00a-45ee-8c17-b27ff0aa9369.sql:26-33`, `20260216014647_97bb0d33-e1ff-4530-a86a-a0ad2637a977.sql:27-41`).
8. `verify-employer` returns `yearsInOperation: string` (`verify-employer/index.ts:71-73`); other numeric business facts in the app are `number`.

---

## 12. Floats for money

**Status:** proposed · **Count:** 14

PostgreSQL money columns are `NUMERIC` (not `float`/`real`) — schema is clean. Application layer uses IEEE-754 `number` and `parseFloat` for currency.

**Types (JS `number` for money)**

1. `src/types/deal.ts:74` `monthlyIncome: number`
2. `src/types/deal.ts:102-110` `loanAmount`, `downPayment`, `tradeInValue`, `apr`, `monthlyPayment`, `totalInterest`, `totalCost`
3. `src/types/deal.ts:148` `fundedAmount?: number`
4. `src/hooks/use-deals.ts:13-19,32` `loan_amount`, `down_payment`, `apr`, `monthly_payment`, `total_interest`, `total_cost`, `funded_amount`
5. `src/components/deals/IncomeSourceCard.tsx:20-21,25,35-37` stated/calculated income, hourly_rate, ytd_gross, manual_override_amount
6. `src/components/deals/ApplicantDebtsCard.tsx:24-25` `monthly_payment`, `total_balance`
7. `src/components/deals/ExtractedDataBadge.tsx:13-14,18` `gross_pay`, `net_pay`, `ytd_gross`
8. Generated `src/integrations/supabase/types.ts` maps every NUMERIC money column to `number` (e.g. `:27`, `:90`, `:266-295`, `:473-480`)

**`parseFloat` on money inputs**

9. `src/components/deals/AddIncomeSourceDialog.tsx:64,69,92,93`
10. `src/components/deals/ApplicantDebtsCard.tsx:98-99`
11. `src/components/deals/IncomeCalculator.tsx:139,148,151-152,164,168-169,175,182,200,210,213-214,234,254-255,260,265,342,345-346,349,481,483,615,617,649,651,694,696,742,745-746,750,996`

**Not counted:** display-only `.toLocaleString()` on already-numeric amounts.

---

## 13. Dates as plain strings not ISO 8601

**Status:** proposed · **Count:** 16

DB columns are `TIMESTAMPTZ` / `DATE` (ISO-capable). Domain types and AI output are unconstrained `string`, so non-ISO values are representable and (for `pay_date`) can be stored after LLM extraction.

1. `src/types/deal.ts:32` `Document.uploadedAt: string`
2. `src/types/deal.ts:43` `DealNote.createdAt: string`
3. `src/types/deal.ts:53` `TimelineEvent.createdAt: string`
4. `src/types/deal.ts:64` `Customer.dateOfBirth?: string`
5. `src/types/deal.ts:116` `CreditInfo.pulledAt: string`
6. `src/types/deal.ts:145` `Deal.decisionAt?: string`
7. `src/types/deal.ts:147` `Deal.fundedAt?: string`
8. `src/types/deal.ts:150-151` `Deal.createdAt` / `updatedAt: string`
9. `src/types/deal.ts:174` `Dealer.createdAt: string`
10. `src/types/deal.ts:185-186` `User.lastLogin?: string` / `createdAt: string`
11. `src/types/deal.ts:197` `Notification.createdAt: string`
12. `src/types/deal.ts:246-248` `IncomeSource.verified_at` / `created_at` / `updated_at: string`
13. `src/components/deals/ExtractedDataBadge.tsx:16,20` `pay_date: string | null`, `extracted_at: string`
14. `src/components/deals/IncomeVerificationCard.tsx:27,31` same
15. `src/components/deals/UnmatchedExtractionRow.tsx:22,26` same
16. `supabase/functions/extract-income-data/index.ts:88-90` tool param `pay_date` type `"string"` (prompt says YYYY-MM-DD, no parse/validate)

Writes that **do** use ISO (`new Date().toISOString()`) are not findings: `IncomeSourceActions.tsx:36,40`, `IncomeCalculator.tsx:256,466,862`, `DealDetail.tsx:119`. Mock timestamps such as `src/data/mockData.ts:16` (`2023-01-15T10:00:00Z`) are already ISO 8601.

---

## 14. External calls without retry / backoff

**Status:** proposed · **Count:** 4

Repo-wide grep for `retry` / `backoff` / `exponential`: no matches.

1. `supabase/functions/extract-income-data/index.ts:42` `fetch("https://ai.gateway.lovable.dev/v1/chat/completions")` — single attempt; 429 returns immediately (`:121-125`)
2. `supabase/functions/verify-employer/index.ts:31` same pattern (`:91-95`)
3. `src/components/deals/DocumentUpload.tsx:91` `supabase.functions.invoke('extract-income-data')` — no retry
4. `src/components/deals/EmployerVerificationCard.tsx:44` `supabase.functions.invoke('verify-employer')` — no retry

PostgREST `supabase.from(...)` calls also have no retry; not counted separately (same client, in-process DB API).

---

## 15. Stale comments

**Status:** proposed · **Count:** 5

1. `src/components/deals/DocumentUpload.tsx:106` “If we have a dealId, save to database” vs `:108-110` “For now we log the extracted data” — first comment is stale; no insert happens (`:110` `console.log` only).
2. `src/components/deals/AddIncomeSourceDialog.tsx:77` “Validate IDs are valid UUIDs for database insertion” vs `:79-80` which **replace** invalid IDs with `crypto.randomUUID()` (orphan FKs), not reject them.
3. `src/hooks/use-deals.ts:139` `documents: [], // loaded separately` — true for `fetchDeals`, but `fetchDealById` (`:170-191`) loads them in the same query; comment is misleading at the shared transformer.
4. `src/pages/DealDetail.tsx:162-165` approve toast “moved to funding review” with no `deals.update` (only auto-decline writes). Comment-level: user-facing copy describes a write that does not exist. Same for `:168-173` decline and `:176-182` add note.
5. `src/integrations/supabase/client.ts:1` “automatically generated. Do not edit it directly” — file is hand-configured auth storage (`:12-16`), not a pure codegen artifact.

Not counted: accurate section comments (`IncomeCalculator` method notes, `Reports.tsx:56` “Mock monthly trend data”).

---

## 16. Unvalidated input

**Status:** proposed · **Count:** 12

No `zod` / schema validation is used despite `zod` in `package.json`.

1. `supabase/functions/extract-income-data/index.ts:15` — `imageBase64` / `imageUrl` / `mimeType` from `req.json()`; no size cap, MIME allowlist, or URL allowlist (`imageUrl` is SSRF-capable at `:39`)
2. `supabase/functions/verify-employer/index.ts:15` — `employer` / `city` / `state` only checks `if (!employer)` (`:17`)
3. `src/components/deals/AddIncomeSourceDialog.tsx:64` `parseFloat(statedIncome)` with no `Number.isFinite`; empty/NaN can persist
4. `src/components/deals/AddIncomeSourceDialog.tsx:79-80` invalid `dealId`/`customerId` swapped for random UUIDs
5. `src/components/deals/ApplicantDebtsCard.tsx:96` `debt_type as any` bypasses enum
6. `src/components/deals/ApplicantDebtsCard.tsx:98-99` `parseFloat` without finite check
7. `src/components/deals/DroppableInput.tsx:34-39` `JSON.parse` of drag payload; any `payload.value` applied
8. `src/components/deals/IncomeCalculator.tsx` (see check 12) — calculator fields written via `parseFloat` / `as any` status enums (`:465`)
9. `src/pages/Auth.tsx:49-55` signup: HTML `required` / `minLength={6}` only; no email-format check beyond `type="email"`
10. `src/pages/DealDetail.tsx:50` `useParams()` `id` used in queries with no UUID check
11. `src/pages/Settings.tsx:26-31,69-80` settings inputs (APR etc.) have no validation; save is local toast only
12. Edge Functions do not validate `Authorization` (see check 17) — unauthenticated callers can POST arbitrary bodies

---

## 17. API routes missing auth

**Status:** proposed · **Count:** 6

Client pages are wrapped in `ProtectedRoute` (`src/App.tsx:34`, `src/components/ProtectedRoute.tsx:15-17`). Edge Functions and several RLS policies are not.

1. `supabase/config.toml:4` `[functions.verify-employer] verify_jwt = false`
2. `supabase/config.toml:7` `[functions.extract-income-data] verify_jwt = false`
3. `supabase/functions/verify-employer/index.ts:9-15` — no `Authorization` / user check; CORS `Access-Control-Allow-Origin: *` (`:4`)
4. `supabase/functions/extract-income-data/index.ts:9-15` — same
5. `supabase/migrations/20260213235900_66b162ae-b00a-45ee-8c17-b27ff0aa9369.sql:26-33` — `extracted_income_data` policies `USING (true)` / `WITH CHECK (true)` with no `TO authenticated` (anon key can read/write)
6. `supabase/migrations/20260216014647_97bb0d33-e1ff-4530-a86a-a0ad2637a977.sql:27-37` — `applicant_debts` SELECT/INSERT/UPDATE `USING (true)` with no `TO authenticated`

Related (not extra counts): many “Authenticated users can …” policies still `USING (true)` for any logged-in user (no role check), e.g. `deals` UPDATE at `20260211053438_f3d9d803-de07-4cda-99ba-4014294f39d6.sql:299`.

---

## 18. Missing indexes

**Status:** proposed · **Count:** 14

**Present:** `extracted_income_data(deal_id, document_id, income_source_id)`; `applicant_debts(deal_id)`; uniques on `dealers.code`, `deals.deal_number`, `profiles.user_id`, `extracted_income_data.document_id`.

**Missing on columns the app filters / joins**

| Table.column | Queried at | Migration that created table (no index) |
|---|---|---|
| `deals.status` | client filters after full fetch `use-deals.ts:238,248` | `20260211053438_…sql:116` |
| `deals.customer_id` | FK join | same `:122` |
| `deals.vehicle_id` | FK join | same `:123` |
| `deals.dealer_id` | FK join | same `:124` |
| `deals.assigned_to` | listed on row type `use-deals.ts:26` | same `:152` |
| `deals.created_at` | `.order('created_at')` `use-deals.ts:165` | same `:169` |
| `documents.deal_id` | `IncomeDocPreview.tsx:99` | same `:176` |
| `deal_notes.deal_id` | nested select `use-deals.ts:173` | same `:188` |
| `deal_timeline.deal_id` | nested select `use-deals.ts:173` | same `:198` |
| `notifications.user_id` | (table exists; UI uses mock) | same `:209` |
| `income_sources.deal_id` | `DealDetail.tsx:81`, `IncomeVerificationCard.tsx:87` | `20260214005858_…sql:11` |
| `income_sources.customer_id` | insert path `AddIncomeSourceDialog.tsx:84` | same `:12` |
| `applicant_debts.customer_id` | insert `ApplicantDebtsCard.tsx:95` | `20260216014647_…sql:11` |
| `vehicles.vin` | displayed, no unique/index | `20260211053438_…sql:106` |

---

## 19. N+1 queries

**Status:** proposed · **Count:** 4

1. `src/components/deals/IncomeVerificationCard.tsx:141-183` — `for (const extraction of unlinked)` issues two writes per row (`.update` extraction + `.update` source). Classic N+1 write loop.
2. `src/components/deals/IncomeSourceCard.tsx` renders `IncomeDocPreview` per source (`IncomeSourceCard.tsx` + `IncomeDocPreview.tsx:81-131`). Each instance fires 4 queries; `income-docs` and `unlinked-extractions-for-drag` repeat per source for the same `dealId`.
3. Duplicate income-source fetches with different keys: `DealDetail.tsx:74-86` (`income-sources-detail`) and `IncomeVerificationCard.tsx:81-92` (`income-sources`) plus `ApplicantDebtsCard.tsx:75` overlapping `DealDetail.tsx:89`.
4. `src/components/deals/IncomeCalculator.tsx:269-443` `handleApply` — sequential read/update of `income_sources` (apply, then variance flag select/update, then MI/YTD flag select/update) plus multiple `deal_timeline` inserts; per-save round-trips scale with flags.

Not N+1: `fetchDeals` uses nested `customers/vehicles/dealers` in one select (`use-deals.ts:154-165`); CreditQueue loads debts with one `.in('deal_id', dealIds)` (`CreditQueue.tsx:28-31`).

---

## 20. SDKs initialized in more than one place

**Status:** none found · **Count:** 0

Single `createClient` at `src/integrations/supabase/client.ts:11`. Single `QueryClient` at `src/App.tsx:23`. Edge Functions do not construct a Supabase client. `useQueryClient()` in `ApplicantDebtsCard.tsx:73` reads the existing context, it does not re-init the SDK.

---

## Summary

| Check | Status | Count |
|---|---|---|
| 1 duplicate utilities | proposed | 12 |
| 2 secrets in config | proposed | 1 |
| 3 functions over 400 lines | proposed | 3 |
| 4 components over 200 lines | proposed | 15 |
| 5 dead code | proposed | 42 |
| 6 silent/empty catch | proposed | 10 |
| 7 UI API calls missing loading/error | proposed | 18 |
| 8 DB queries in route handlers | proposed | 2 |
| 9 sync I/O in request handlers | none found | 0 |
| 10 list endpoints without pagination | proposed | 12 |
| 11 inconsistent API shapes | proposed | 8 |
| 12 floats for money | proposed | 14 |
| 13 dates as plain strings not ISO 8601 | proposed | 16 |
| 14 external calls without retry/backoff | proposed | 4 |
| 15 stale comments | proposed | 5 |
| 16 unvalidated input | proposed | 12 |
| 17 API routes missing auth | proposed | 6 |
| 18 missing indexes | proposed | 14 |
| 19 N+1 queries | proposed | 4 |
| 20 SDKs initialized in more than one place | none found | 0 |

**Totals:** 18 proposed, 2 none found. Highest-severity clusters to treat first in a later hardening pass (not done here): Edge Function `verify_jwt = false` + CORS `*`, `.env` tracked with the anon key, UUID substitution on income-source insert, unbounded deal lists, and money as JS `number`/`parseFloat`.
